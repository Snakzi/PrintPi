# Only on the Pi: both build a C extension and are useless elsewhere.
rpi_ws281x>=5.0; sys_platform == "linux" and platform_machine in "aarch64 armv7l armv6l"
spidev>=3.6; sys_platform == "linux" and platform_machine in "aarch64 armv7l armv6l"
